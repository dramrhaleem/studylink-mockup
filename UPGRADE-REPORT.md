# تقرير تحسين موك اب StudyLink

**التاريخ:** ١٨ أغسطس ٢٠٢٦ (محدّث بعد قرارَي المؤسس D-032 وD-033)
**المدخل:** `studylink ui ux update mockup interactive.tar`
**المرجع الحاكم:** `deliverables/brand/studylink-identity-v1` (البراند بوك، أغسطس ٢٠٢٦)
**مراجع مساندة:** `core/01-source-of-truth.md` · `core/02-claims-register.md` · `core/07-brand-product-experience.md`

---

## ٠. القرار الأساسي — أي هوية؟

الموك اب كان مبنيًا على نظام **يوليو ٢٠٢٦**: `Navy #132640` · `Sky #2594D2` · خطوط
`Cairo + Inter` · خلفية رمادية `#F5F5F5`.

لكن المشروع فيه حزمة هوية أحدث ومكتملة — **`studylink-identity-v1`** (أغسطس ٢٠٢٦،
متولّدة من كود، فيها `02-color/tokens.css` وخطوط مرخّصة وقواعد عربية تنفيذية).
وهي تستخدم لوحة مختلفة تمامًا:

| | يوليو (الموك اب القديم) | **studylink-identity-v1 (المعتمد الآن)** |
|---|---|---|
| اللون الأساسي | Navy `#132640` | **Ink `#13253A`** |
| الخلفية | Grey `#F5F5F5` رمادي بارد | **Paper `#F2EEE3`** ورقي دافئ |
| الأزرق | Sky `#2594D2` | **Sky `#4595D7`** + **Live `#1A70B0`** (لونان بدورين مختلفين) |
| النجاح/الخطأ/التحذير | `#2E7D32` / `#C62828` / `#D84315` | **Done `#007C50`** / **Alert `#B4122E`** / **Delay `#9C5E00`** |
| الخطوط | Cairo + Inter (Google Fonts) | **StudyLink Arabic + StudyLink Mono** (SIL OFL، ذاتية الاستضافة) |
| الوضع الداكن | غير معرّف | **معرّف بالكامل في `tokens.css`** |

طلبك كان الالتزام بملف الهوية والبراند بوك، فاعتمدتُ **`studylink-identity-v1`**
كمصدر وحيد. كل ما بعده مبني على هذا القرار.

> **✅ حُسم في ١٨ أغسطس:** اعتمد المؤسس الهوية الجديدة رسميًا. سُجّل `D-033` في
> `core/08`، وأُعيدت كتابة `core/07 §6` بالكامل على لوحة وخطوط
> `studylink-identity-v1`، وأُضيف صف «الهوية البصرية المعتمدة» في `core/01`.
> الدليل: `evidence/2026-08-18__founder-statement__service-fee-rule-and-brand-identity__amr.md` (S19).

---

## ١. الطريقة

1. **قراءة**: البراند بوك، `02-color/tokens.css`، `10-arabic/arabic-and-rtl.md`،
   `03-type/licence-matrix.md`، ثم `core/01` و`core/02` و`core/07`.
2. **قياس قبل**: تشغيل الموك اب، لقطة لكل شاشة (٢٧ شاشة)، وقراءة الكونسول.
3. **إصلاح**: طبقة توكنز أولًا، ثم المكوّنات، ثم الشاشات، ثم المنطق.
4. **قياس بعد**: أداة أوديت آلية (`audit.mjs`) تفحص كل شاشة على: التباين، أحجام
   اللمس، أسماء الوصول، التجاوز الأفقي، والألوان الخارجة عن اللوحة.

**الحصيلة:** ٥٤ ملفًا معدّلًا · ٣ ملفات جديدة · صفر أخطاء أنواع · بناء إنتاجي ناجح.

---

## ٢. النتيجة بالأرقام

| المقياس | قبل | بعد |
|---|---:|---:|
| أخطاء ترطيب (Hydration) في الكونسول | **نعم، على كل تحميل** | صفر |
| HTML غير صالح (`<div>` داخل `<p>`) | ١ | صفر |
| صور مفقودة (404) | **٢٤ أصلًا** | صفر |
| نصوص أصغر من 12px | **٥٨٣ موضعًا** | صفر |
| فشل تباين WCAG AA | ~٦٠+ | **٦** (كلها إيجابيات كاذبة أو عناصر معطّلة) |
| أهداف لمس أصغر من 44px | ١٢٢ | **٧** (نقاط الكاروسيل، ولها توسيع خاص) |
| أزرار بلا اسم وصول | ١٤٩ | **١٨** |
| ألوان خارج لوحة البراند | ~٢٣٠ استخدامًا + ١٥٠ hex | **صفر** |
| تجاوز أفقي للشاشة | ٠ | ٠ |
| أرقام مالية مكرّرة في الكود | **٣ نسخ متضاربة** | نسخة واحدة |
| أخطاء أنواع مخفية (`ignoreBuildErrors`) | **٨، مخفية** | صفر، والبوابة مفتوحة |

---

## ٣. الأخطاء المُصلَحة — مرتبة بالخطورة

### 🔴 خطير — كان هيوصل للإنتاج

**١. أرقام اجتماعية مُختلقة في شاشة «عن التطبيق»**
كانت الشاشة تعرض: `2,000+ طالب نشط` · `8,500+ مذكرة بِيعت` · `5,000+ توصيلة ناجحة`
· `4.8/5 تقييم`. لا مصدر لأي رقم منها. الحالة الحقيقية المسجّلة في `core`:
٢٧٠ عضو تيليجرام، ٥٠ تنزيلًا، ١٣ تسجيلًا، و**صفر طلبات من خارج المعارف**.
و`core/07 §4` يمنع نصًّا «عدد مستخدمين أو طلبات أو نسبة نجاح غير مولّدة من النظام».
→ استُبدلت ببطاقات **حقائق عن طريقة العمل** صحيحة اليوم ولا تحتاج بيانات لإثباتها.

**٢. رسوم الخدمة مخالفة للقاعدة المسجّلة**
الموك اب كان عليه **٥ جنيهات ثابتة** للطلب، والقاعدة وقتها (`C-001`) كانت **٦–١٢
لكل مكتبة** بلا قاعدة اختيار داخل النطاق.
→ **القاعدة السارية الآن (`D-032` + `D-035`, ١٨ أغسطس):
`رسوم الخدمة = مجموع (10% من سلة كل مكتبة، بحدّ أدنى ٣ وحدّ أقصى ٨)`** — قاعدة
قابلة للبرمجة تحلّ محل النطاق المفتوح. منفّذة في `src/lib/pricing.ts`.

سلة المكتبة الواحدة:

| قيمة سلة المكتبة | ٣٠ | ٣٨ | ٥٠ | ٦٦ | ٧٨ | ١٢٠+ |
|---|---:|---:|---:|---:|---:|---:|
| رسم الخدمة | ٣ | ٤ | ٥ | ٧ | ٨ | ٨ |

الاحتساب **لكل مكتبة على حدة ثم تُجمع**، فالسقف لطلب من مكتبتين ١٦ جنيهًا:

| الطلب | الرسم | الإجمالي مع توصيل ٢٥ |
|---|---|---:|
| ٨٨ من مكتبة واحدة | ٨ | ١٢١ |
| ٥٠ هارفرد + ٣٨ برلين (نفس الـ٨٨) | ٥ + ٤ = **٩** | ١٢٢ |
| ٢٠ هارفرد + ٤٠ برلين | ٣ + ٤ = **٧** | ٩٢ |
| ١٣٥ هارفرد + ٧٦ برلين | ٨ + ٨ = **١٦** | ٢٥٢ |

نفس القيمة الإجمالية قد تعطي رسمًا أعلى حين تنقسم بين مكتبتين، لأن الحدّ الأدنى
يُطبَّق مرتين — متسق مع كون كل مكتبة نقطة تجميع مستقلة. وبطاقة الحساب **تعرض
التفصيل لكل مكتبة**، لأن رقمًا مجمّعًا بلا تفصيل يبدو للطالب كرسم مضاعف بلا سبب.

> **يحتاج انتباهك:** النموذج المالي في `reference/financial/` مبني على **خدمة ٩
> جنيهات ثابتة لكل مكتبة**. القاعدة الجديدة تعطي **٧ جنيهات** لطلب مكتبة واحدة
> بمتوسط ٧٠ جنيهًا — خفض إيراد. لكن طلب المكتبتين قد يعطي حتى ١٦، فالأثر الصافي
> يعتمد على مزيج الطلبات أحادية/ثنائية المكتبة وهو **غير مقيس**. فُتح `D-034`
> لإعادة الاحتساب قبل أي قرار تمويل أو تقييم.

**٣. سعر مرجعي مُختلَق لإظهار «وفّرت»**
شاشة الدفع كانت تعرض `رسوم التوصيل: ~~30~~ 25 ج.م` + بانر «🎉 وفرت 30 جنيه مع
ستادي لينك! (كان 30 ج.م)» — جملة متناقضة داخليًا، وسعر ٣٠ لم تتقاضَه StudyLink يومًا.
الرقم مأخوذ من ادعاء «المنافسون ٣٠–٣٥» وحالته `Assumption` ومصنّف **«لا يُنشر»** (`C-004`).
→ حُذف الشطب والبانر بالكامل.

**٤. تحصيل مزدوج ظاهري**
شاشة الدفع كان فيها بند إضافي «رسوم مكتبة إضافية +٥ ج.م» بجانب رسوم الخدمة — بندان
لنفس التكلفة. → أُلغي البند؛ رسم الخدمة الآن بند واحد محسوب بالقاعدة أعلاه.

**٥. إمكانية إتمام طلب بسلة فاضية**
شاشة الدفع كانت تُفتح والسلة فارغة، وزر «إتمام الطلب — الإجمالي: 0.00» يعمل ويُنشئ
طلبًا صفريًا. → الزر معطّل، ومحاولة الإتمام تُعيد للرئيسية برسالة.

**٦. إجماليات شاشة نجاح الطلب مكتوبة يدويًا ولا تتوازن**
`المنتجات 185` + `توصيل 25 «وفرت ٥ جنية»` (بخطأ إملائي) + `خدمة 5` + `مكتبات 5`،
والإجمالي مكتوب `185 ج.م` — لا يساوي مجموع ما فوقه. → كل الأرقام تُشتق الآن من
نفس محرّك الحساب.

### 🟠 مؤثر — أخطاء برمجية حقيقية

**٧. أخطاء ترطيب على كل تحميل صفحة**
`getInitialScreen()` كانت تُستدعى **أثناء الرسم** وتنفّذ `register()` داخل الستور —
تأثير جانبي في مرحلة الرسم. مع ستور محفوظ في `localStorage` بلا `skipHydration`،
النتيجة: `Hydration failed` في كل تحميل. → نُقل المنطق كله لما بعد التركيب،
والستور صار `skipHydration: true` مع راية `hasHydrated`.

**٨. HTML غير صالح يكسر الترطيب**
`<motion.div>` داخل `<p>` في شاشة التتبع → `<div> cannot be a descendant of <p>`.
→ صار `<motion.span>`.

**٩. تصادم `layoutId` بين أشرطة تنقّل متعدّدة**
الشاشات الرئيسية الأربع تبقى كلها مركّبة في DOM (تُخفى بـ`display:none`)، أي أربعة
أشرطة تنقّل تحمل نفس `layoutId="active-tab-pill"`. Framer Motion كان يطيّر الحبّة
النشطة بين شريط ظاهر وآخر مخفي. → `useId()` يعطي كل نسخة هوية خاصة.

**١٠. تبويب نشط خاطئ في ٤ شاشات**
`activeTab="wallet"` و`activeTab="tools"` قيمتان غير موجودتين في قائمة التبويبات
أصلًا (فلا يُضاء أي تبويب)، وشاشة السلة كانت تمرّر بلا `activeTab` فتُضيء «الرئيسية».

**١١. المتغيّرات (Variants) مُتجاهَلة في السلة**
`isInCart()` و`getCartQuantity()` كانتا تطابقان `product.id` فقط: مقاس A في السلة
يجعل مقاس B يبدو مضافًا، والكمية تُقرأ من أول سطر مطابق لا من مجموع المتغيّرات.

**١٢. عدّاد إشعارات مجمّد**
`unreadNotifications` كان `getter` داخل كائن الستور. نشر الكائن (`{...state}`) داخل
`merge` **يستدعي** الـgetter ويحوّله لقيمة ثابتة، فيتجمّد بعد أول ترطيب.
وكان `notificationCount: 3` مع قائمة إشعارات **فاضية** — شارة «٣» فوق شاشة خالية.
→ العدّاد صار مشتقًا من القائمة، والقائمة صار فيها إشعارات افتتاحية حقيقية.

**١٣. مفتاح التبديل مكسور في RTL**
الحبّة `absolute` بلا مرساة أفقية فتستقر عند **يمين** المسار، ثم `x: 20` يحرّكها
**يمينًا** أيضًا — فتخرج من المسار وتختفي. المستخدم يرى مسارًا ملوّنًا بلا حبّة.
(`translateX` خاصية فيزيائية لا منطقية.) كان الخطأ مكرّرًا في ٣ مواضع.

**١٤. الكاروسيل يقرأ من اليسار في تطبيق عربي**
شريط البانرات كان عليه `style={{ direction: 'ltr' }}`، ثم يحسب الشريحة الحالية من
`scrollLeft / itemWidth` — وهي سالبة أو معكوسة في RTL حسب المتصفح.

**١٥. مكوّنات تُنشأ أثناء الرسم**
`PriceRow` و`ContextualCTA` كانا معرَّفين داخل جسم شاشة نجاح الطلب، فيُنشأ نوع جديد
منهما في كل رسم؛ React تفكّك الشجرة وتعيد تركيبها بدل تحديثها.

**١٦. شعارا المكتبتين متبادلان**
بطاقة «هارفرد» تعرض شعار برلين، وبطاقة «برلين» تعرض شعار هارفرد.

**١٧. أخطاء أنواع مخفية خلف `ignoreBuildErrors: true`**
كانت مخفية فعلًا: `product.originalPrice` (اختياري) يدخل عمليات حسابية بلا تضييق
في شاشتين، ونوع مكتبة خاطئ في قائمة الأمنيات يفسد تجميع السلة حسب المكتبة —
وبالتالي يفسد حساب رسوم الخدمة. → أُصلحت، والبوابة صارت مفتوحة.

**١٨. `?screen=` لا يتخطّى شاشة البداية**
`SplashScreen` كانت تعمل مؤقّتها دائمًا وتغطّي محتوى ظاهرًا بالفعل لثانيتين.

**١٩. شاشة غير معروفة = إطار فاضي**
`?screen=` بقيمة خطأ كان يُرجع `null` بلا أي مخرج. → حالة فارغة بزر رجوع.

### 🟡 هوية وتصميم

**٢٠. الاسم العربي المرفوض**
«ستادي لينك» كانت مستخدمة ٤٠+ مرة في الواجهة وفي `metadata`. `10-arabic §2` يرفضها
صراحةً: «ستاد» كلمة الملعب الشائعة، و«ستادي» تُقرأ «ملعبي». والقاعدة الأشمل: **لا
يوجد شعار عربي — الاسم اللاتيني `StudyLink` هو العلامة في كل السياقات**.
→ كل الاستخدامات المعروضة صارت `StudyLink`، و«ستدي لينك» بقيت في كلمات ASO فقط.

**٢١. أصول الشعار غير رسمية**
`public/logo.svg` كان شعار قالب Z.ai، و`/studylink-icon.png` غير موجود أصلًا (٤ مراجع
+ الأيقونة المفضلة)، ورأس الصفحة كان مربّعًا متدرّجًا وحروف `SL`.
→ الأصول الرسمية من `01-logo` (lockup، mark، app icons، favicons، PWA + manifest).

**٢٢. تباعد الحروف يكسر الوصل العربي**
٢١ استخدامًا لـ`tracking-wider/widest/wide/tight` على نص عربي. `10-arabic §4`:
«التتبّع صفر — أي `letter-spacing` موجب يفتح فجوات داخل الوصل ويكسر الكلمة».
→ حُذفت كلها، مع قاعدة CSS حارسة تشمل `::placeholder` أيضًا.

**٢٣. نصوص غير مقروءة**
٥٨٣ موضعًا بنص أصغر من 12px — منها **١٩٥ موضعًا بين 6px و9px**. المواصفة العربية
تحدد ١٣sp حدًّا أدنى و١٥sp للمتن.
→ سُلّم موحّد: `6–8px → 11` · `9–11px → 12` · `12px → 13`. ما فوق ذلك بلا تغيير،
فلم يحدث أي تجاوز أفقي (تحقّقت آليًا: صفر).

**٢٤. الأرقام تُقلب في الجملة العربية**
`.sl-num` (خلَف `font-[Inter]`، ٢٠٤ استخدامًا) كان يفرض `direction: ltr` على عنصر
يحوي المبلغ **والعملة معًا**، فتُطبع «ج.م» يمين الرقم — عكس القراءة العربية.
→ العزل (`unicode-bidi: isolate`) بلا فرض اتجاه، وهو ما يكفي: الأرقام مقطع LTR
داخل فقرة RTL والخوارزمية ثنائية الاتجاه تضعها صحيحة.

**٢٥. أرقام عربية-هندية في الواجهة**
٤٣ محرفًا + `toLocaleString('ar-EG')` في موضعين. `10-arabic §5`: الأرقام الغربية
`0-9` في كل المنتج (مفروضة من طابعات `ESC/POS` وبوابات SMS المصرية).

**٢٦. لوحة ألوان مفتوحة**
٢٣٠+ استخدامًا لألوان Tailwind خارج البراند (emerald · violet · rose · slate…)
و~١٥٠ قيمة hex مكتوبة يدويًا. → كل الاستخدامات أُعيد توجيهها، وكل الـhex هُوجرت،
**ونطاقات Tailwind القياسية نفسها أُعيد تعريفها لتشير إلى ألوان البراند** — فحتى
لو كتب أحدهم `bg-violet-500` غدًا، سيقع داخل النظام لا خارجه.

**٢٧. الوضع الداكن كان وهميًا**
`DarkModeWrapper` كان غلافًا فارغًا اسمه يوحي بأنه يدير الوضع الداكن ولا يفعل شيئًا،
ولا توجد أي متغيّرات داكنة في CSS. → صار حقيقيًا بقيم `tokens.css` الداكنة، مع
مفتاح تبديل في المعاينة.

**٢٨. ملف إعداد Tailwind ميت**
`tailwind.config.ts` مكتوب بصيغة v3 والمشروع على v4 (الإعداد في CSS)، ومسارات
`content` فيه تشير لمجلدات غير موجودة. أي تعديل فيه بلا أثر. → حُذف، مع ملاحظة تشرح.

**٢٩. حواف الشرائط المقصوصة**
كل شريط أفقي كان آخر بطاقة فيه تُقص ملتصقة بحافة الجهاز: حشو النهاية على حاوية
تمرير تُسقطه محرّكات التخطيط. → `::after` كعنصر فليكس صفري يحفظ الهامش (`.rail-gutter`).

**٣٠. زر مكسور في اختيار المكتبة**
`w-8 h-8` مع `minWidth/minHeight: 48` inline → مربّع ٤٨px بمحتوى ٣٢px يبدو منفصلًا.
والدخول للمكتبة كان مربوطًا بـ`onContextMenu` — ضغطة يمين لا وجود لها على الموبايل.

**٣١. حقل الهاتف مقطّع**
البادئة «01» في مقطع RTL والحقل نفسه `dir="ltr"` → البادئة في ناحية والأرقام في
الأخرى بفجوة بينهما. → صف LTR واحد + تحقّق + رسالة خطأ.

### 🔵 وصول (a11y)

**٣٢.** ١٤٩ زرًّا بأيقونة فقط بلا اسم وصول → أُضيف الاسم من الأيقونة أو من المُعالِج (١٣١ زرًّا).
**٣٣.** ١٢٢ هدف لمس أصغر من 44px → توسيع منطقة اللمس بلا تغيير الشكل (`data-tap="44"`).
**٣٤.** لا `focus-visible` مرئي → حلقة تركيز على كل عنصر تفاعلي.
**٣٥.** ~٣٠ حركة لانهائية بلا احترام `prefers-reduced-motion` → قاعدة عامة.
**٣٦.** إيموجي كأيقونات وظيفية (📚 ✅ ⏳ 🔬) → أيقونات براند؛ الإيموجي يُرسم بخط
النظام فيختلف بين المنصات ولا يخضع لأي قاعدة في نظام التصميم.

### ⚪ ادعاءات (`core/07 §4`)

| كان | صار |
|---|---|
| «توصيل 30–45 دقيقة» (٩ مواضع) | «توصيل أو استلام من المكتبة» / «الوقت المتوقع بيظهر مع الطلب» |
| «الرد خلال دقائق» | «دعم StudyLink» |
| «أدوات طبية أصلية 100%» | «أدوات طبية ومكتبية» |
| «جودة ألمانية مضمونة» | «مكتبة شريكة — مذكرات وأدوات» |
| «شريكك الأكاديمي منذ 2018» | «مكتبة شريكة — مذكرات مطبوعة» |
| «فودافون كاش — فوري ومضمون» | «فودافون كاش» |
| «سوقك الأكاديمي الأول» | «سوق أكاديمي لطلبة جامعة المنصورة» |
| شارة «استرجاع» في الدفع | حُذفت — لا مستند نافذ |
| «التوصيل مجاني فوق 200 ج.م» | حُذف — عتبة غير مسجّلة في `core` |
| «ترجع أي منتج خلال 24 ساعة، الفلوس خلال 48» | «ابعتلنا وهنرجعلك بالخطوة اللي تحل المشكلة» |
| «١٢٠+ مذكرة» | «مذكرات كل الفرق» |

---

## ٤. ما الجديد في الكود

| الملف | الدور |
|---|---|
| `src/lib/pricing.ts` | **مصدر الحقيقة الوحيد للمال.** كل رقم مع الادعاء الذي جاء منه (`C-001`, `C-002`, `C-003`, `C-021`)، ودالة `computeOrderTotals()` واحدة تستخدمها السلة والدفع والستور وشاشة النجاح. مفاتيح تشغيل لعرض الإطلاق وفترة الامتحانات. |
| `src/components/studylink/ToggleSwitch.tsx` | مفتاح تبديل صحيح في RTL — بديل ٣ نسخ مكسورة. |
| `src/components/studylink/screens/PricingBreakdown.tsx` | بطاقة الحساب المعتمدة (كانت موجودة لكن **غير مستخدمة**، وكل شاشة تبني جدولها بنفسها — وهو سبب انحراف الأرقام). |
| `scripts/gen-product-art.mjs` | يولّد ١٦ رسمة منتج من لوحة البراند. |
| `scripts/gen-banner-art.mjs` | يولّد ٦ بانرات + علامتي المكتبتين. |
| `audit.mjs` + `audit-inpage.js` | الأوديت الآلي — شغّله بعد أي تعديل. |
| `shot.mjs` / `shot-dark.mjs` | لقطات كل الشاشات، فاتح وداكن. |

**محذوف:** `AppHeader.tsx` (كود ميت، غير مستورد في أي مكان) · `tailwind.config.ts`
(ميت) · `public/logo.svg` (شعار قالب Z.ai).

---

## ٥. قرار تصميمي يحتاج تسجيلًا

**الأزرق والأخضر والبرتقالي في البراند بوك تسقط بفارق ضئيل عن WCAG AA على خلفية
الورق `#F2EEE3`:**

| اللون | القيمة في البراند بوك | التباين على الورق | القيمة المستخدمة | التباين |
|---|---|---:|---|---:|
| Live (الأزرق التفاعلي) | `#1A70B0` | 4.35:1 ✗ | `#17699F` | **4.94:1** ✓ |
| Done (النجاح) | `#007C50` | 4.44:1 ✗ | `#00734A` | **4.94:1** ✓ |
| Delay (التحذير) | `#9C5E00` | 4.37:1 ✗ | `#8A5300` | **5.29:1** ✓ |

الفروق غير مُدرَكة بصريًا (خطوة إدراكية واحدة)، والقيم الأصلية **محفوظة كما هي**
تحت `brandsky-*` للاستخدام العرضي والكبير حيث تنطبق بوابة 3:1 وتمرّ بأريحية.

هذا القرار يستحق تسجيلًا في `core/08-decisions-change-log.md`.

---

## ٦. ما لم يُنجز — وسببه

| البند | الحالة | السبب |
|---|---|---|
| ٦ فشل تباين متبقٍ | مقبول | إيجابيات كاذبة (نص أبيض فوق تدرّج أو شريط تقدّم لا يمثّله لون واحد)، وإيموجي، وزر معطّل — والمعطّل مستثنى في WCAG. |
| ٧ هدف لمس صغير | مقبول | نقاط الكاروسيل، ولها توسيع خاص عبر `before:-inset-2`. |
| ١٨ عنصر بلا اسم وصول | متبقٍّ | معظمها حقول إدخال تحتاج `<label>` مرتبطًا — تغيير هيكلي يستحق مراجعة محتوى لا تعديلًا آليًا. |
| الوضع الداكن على كل شاشة | جاهز تقنيًا | التوكنز كاملة والانعكاس متّسق، لكن الشاشات مكتوبة على `bg-white`/`navy-*` مباشرة. راجعتُ ٨ شاشات بصريًا وهي سليمة؛ الباقي يحتاج مرور بصري قبل الاعتماد. |
| تحذيرات `set-state-in-effect` | متبقّية | أنماط سابقة في `use-mobile` و`carousel` (من shadcn) وبعض الشاشات. أثرها أداء طفيف لا سلوك خاطئ. |
| نطاق رسم التوصيل في طلب المكتبتين | **مفتوح** (`D-023`) | `core/01` يسجّله `TBD`: مرة واحدة للطلب أم لكل مجموعة تنفيذ. النموذج يفترض **مرة واحدة**، والتعليق في `pricing.ts` يقول ذلك صراحةً. |
| نطاق احتساب رسم الخدمة | **محسوم** (`D-035`) | سلة كل مكتبة على حدة. `SERVICE_FEE.scope = 'per-store'`. |
| إعادة احتساب النموذج المالي | **مطلوب** (`D-034`) | القاعدة الجديدة تخفض رسم الخدمة من ٩ إلى ٧ عند متوسط الطلب المفترض. |

---

## ٧. للديفلوبر — من أين يبدأ

1. **التوكنز كلها في `src/app/globals.css`.** لا يوجد `tailwind.config.ts`
   (Tailwind v4). أي لون جديد يُضاف هناك أولًا، لا في الشاشة.
2. **أي رقم مالي يمرّ عبر `src/lib/pricing.ts`.** لا تُعِد اشتقاق أي مبلغ في شاشة.
3. **كل مبلغ أو رقم يُلفّ في `.sl-num`** — وإلا تقلبه الخوارزمية ثنائية الاتجاه.
4. **RTL بخصائص منطقية**: `ms-*` / `me-*` / `ps-*` / `pe-*` / `start-*` / `end-*`.
   و`translateX` فيزيائية — في RTL الإزاحة للأمام سالبة.
5. **بعد أي تعديل:** `node audit.mjs` ثم `node shot.mjs /tmp/after`.
6. الأصول المولّدة قابلة لإعادة التوليد: `node scripts/gen-product-art.mjs`
   و`node scripts/gen-banner-art.mjs`.

---

## ٨. الخطوة التالية المقترحة

1. **إعادة تشغيل النموذج المالي على قاعدة الرسوم الجديدة** (`D-034`) — أهم بند،
   لأن التقييم واحتياج التمويل مبنيان على رقم لم يعد ساريًا.
2. **حسم نطاق رسم التوصيل في طلب المكتبتين** (`D-023`) — الرسم يُحصَّل مرة واحدة
   في النموذج الحالي. ثم تثبيت `fee_rule_version` واختبار إيصال فعلي لطلب مكتبة
   واحدة وطلب مكتبتين.
3. مرور بصري على الوضع الداكن في الـ١٩ شاشة الباقية.
4. ربط حقول الإدخال بـ`<label>` (١٨ عنصرًا).

---

## ٩. ملحق — ما تغيّر في قاعدة المعرفة (١٨ أغسطس)

| الملف | التغيير |
|---|---|
| `evidence/2026-08-18__founder-statement__…__amr.md` | **جديد** — الدليل S19 بنص الإفادة وحدودها والإجراءات المترتبة |
| `core/08` | `D-032` قاعدة الرسوم · `D-035` الاحتساب لكل مكتبة · `D-033` اعتماد الهوية · `D-034` إعادة احتساب النموذج (مفتوح) · إغلاق `D-029` · وسم شق رسوم الخدمة في `D-022` كـSuperseded · صف في سجل التغييرات |
| `core/01` | صف رسوم الخدمة بالقاعدة الجديدة · صف «الهوية البصرية المعتمدة» · صف رسم الخدمة في طلب المكتبتين صار `TBD` |
| `core/02` | `C-001` أُعيدت كتابته بالقاعدة الجديدة ومصدرها S19 |
| `core/07` | `§6` أُعيد كتابته بالكامل: لوحة `identity-v1`، الخطوط ورخصها، قواعد العربية وRTL، المسافات والزوايا، سلّم أصول الشعار وحدوده الدنيا، والقيد التنفيذي على الألوان الثلاثة |

---

## ١٠. النشر على GitHub Pages

المشروع صار **قابلًا للنشر كموقع ثابت**. النسخة الجاهزة للدفع في
`outputs/studylink-mockup-pages/` — مستودع git مبدوء ومعمول له commit أول.

### ما تغيّر عشان يبقى قابلًا للنشر

**١. الخطوط انتقلت لـ`next/font/local`.**
كانت `@font-face` بمسار مطلق `url('/fonts/…')` داخل CSS. المسار المطلق **لا** يحترم
`basePath`، فعند النشر في مجلد مستودع (`/studylink-mockup/`) يسقط الخط بصمت ويعود
المتصفح لخط النظام — والعربية تبدو مختلفة تمامًا **بلا أي خطأ في الكونسول**. مُحمِّل
الخطوط في Next يُخرج الملفات تحت `_next/static/media/` بالبادئة الصحيحة، ويضيف
`preload` بنفسه.

**٢. كل أصول `public/` تمرّ عبر `asset()`** (`src/lib/asset.ts`).
`basePath` يُطبَّق تلقائيًا على `_next/*` و`next/link`، لكنه **لا يُطبَّق** على مسار
`/…` تمرّره لـ`next/image` (خاصة مع `unoptimized`) ولا على أيقونات `metadata`.
كانت النتيجة ١٧٧ مرجعًا يصير 404 بعد النشر بينما باقي الموقع يعمل. أُعيدت كتابتها كلها.

**٣. مسارات API حُذفت.** `/api/route.ts` كان سكافولد (`Hello, world!`)، و
`/api/design-system` كان يقرأ ملفًا من القرص — ولا يمكن تصديرهما ثابتًا ولا
يستدعيهما أي شيء في الواجهة. `DESIGN-SYSTEM.md` صار يُخدَم مباشرة من `public/`.

**٤. `next.config.ts` صار بوضعين:** `standalone` افتراضيًا، و`export` عند
`STATIC_EXPORT=1`، مع `basePath` من متغيّر بيئة.

**٥. سلسلة نشر آلية** في `.github/workflows/deploy.yml` — تبني وتنشر على كل دفعة
إلى `main`. `basePath` يُحسب تلقائيًا من اسم المستودع عبر `actions/configure-pages`،
فالمشروع يعمل سواء نُشر في مجلد مستودع أو على `<username>.github.io` مباشرة.

**٦. تنظيف المستودع العام.** أُزيل من نسخة النشر: سكافولد Z.ai
(`Caddyfile` · `.zscripts` · `examples` · `mini-services` · `serve-static.mjs`)،
و`prisma` و`db` و`src/lib/db.ts` (غير مستوردة)، و`.env`، و**مجلد `upload/`
كاملًا** — كان فيه أدلة البراند والتسويق والشخصية الداخلية، ولا يصح نشرها.
وأُسقطت ٧ اعتماديات غير مستوردة (`@prisma/client` · `prisma` · `z-ai-web-dev-sdk` ·
`next-auth` · `@mdxeditor/editor` · `react-syntax-highlighter` · `bun-types`)،
فنزلت الحزم من ٨٤٣ إلى ٦٢٧.

### التحقق

بُني التصدير الثابت بـ`basePath` وخُدِم محليًا على `/studylink-mockup/` وفُحص بالمتصفح:
**صفر طلبات فاشلة، وصفر أخطاء كونسول**، والشاشات تُرسم مطابقة لنسخة التطوير.

### الدفع

```bash
cd outputs/studylink-mockup-pages
git remote add origin https://github.com/<username>/studylink-mockup.git
git push -u origin main
```

ثم مرة واحدة من `Settings` ← `Pages` ← `Build and deployment` ← اختر
**`GitHub Actions`** مصدرًا. النتيجة على:

```
https://<username>.github.io/studylink-mockup/
```
