<div dir="rtl">

# قانون اللون — StudyLink

> هذا الملف يجيب عن سؤال واحد: **متى** يُستخدم اللون. أما **أي** لون موجود
> فمكانه `src/app/globals.css` داخل `@theme`.

---

## المشكلة التي حلّها هذا القانون

اللوحة كانت **مغلقة تقنيًا** بالفعل: كل نطاقات Tailwind القياسية
(`emerald` · `violet` · `rose` …) مُعاد تعريفها لتشير إلى ألوان البراند، فلا
يمكن لصنف شارد أن يخرج عن النظام. ومع ذلك بدت الواجهة «كمدينة ملاهٍ».

السبب أن الإغلاق التقني لا يمنع الفوضى الدلالية. القياس قبل الإصلاح:

| الشاشة | عدد العائلات اللونية في شاشة واحدة |
|---|---|
| `LibraryScreen` | amber · emerald · rose · sky · violet |
| `NotificationsScreen` | amber · emerald · error · sky · success · violet |
| `HomeScreen` | amber · emerald · error · sky · success · teal |

و**٢٦١ إيموجي** في الواجهة، كل واحد منها لون خارج اللوحة لا يمكن ضبطه.

أسوأ حالة وُجدت: لون خلفية بطاقة المنتج كان يُختار بـ

```ts
const gradients = ['from-sky-100…','from-emerald-100…','from-amber-100…',
                   'from-rose-100…','from-violet-100…']
gradients[product.id.charCodeAt(product.id.length - 1) % 5]
```

أي أن لون البطاقة يعتمد على **آخر حرف في المعرّف**. لون بلا معنى، يتغيّر
بتغيّر رقم المنتج. هذا هو مصدر الإحساس بالعشوائية.

---

## القانون

### ١. الحبر والورق يحملان الواجهة

`navy-*` للنص والأسطح القوية · `brand-grey-*` للخلفية والحدود والنص الثانوي.

**اختبار عملي:** أزل كل الألوان الأخرى ذهنيًا. لو صارت الشاشة غير مفهومة،
فأنت تعتمد على اللون بدل التسلسل الهرمي.

### ٢. `sky-*` هو اللهجة التفاعلية الوحيدة

كل ما يُنقر وليس زرّ الإجراء الأساسي = سماوي. **لا لون آخر يعني «اضغط هنا».**

### ٣. ألوان الحالة للحالة فقط

| اللون | المعنى | أمثلة صحيحة |
|---|---|---|
| `success` | تمّ · متاح · مفتوح | «تم التسليم» · «مفتوح الآن» · «وفرت ١٣ ج.م» |
| `warning` | انتظار · تنبيه · مغلق مؤقتًا | «قيد المراجعة» · «مغلقة الآن» |
| `error` | خطأ · حذف · نفد | «غير متوفرة» · زر الحذف · «ملغي» |

**ممنوع استخدامها للزينة.** «جديد» ليست حالة نجاح — هي معلومة، فتأخذ السماوي.
(كانت خضراء، وقد صُحّحت.)

### ٤. `amber-*` (HILITE) لشيئين فقط

مكافأة السفير · شارة «أدوات مكتبية». لا ثالث لهما.

### ٥. لون التصنيف من `src/lib/category.ts` وحده

ثلاثة أزواج، كلها **AAA (≥ 7:1)** لأن الشارة نص صغير فوق تعبئة ملوّنة:

| التصنيف | التعبئة | النص | التباين |
|---|---|---|---|
| محاضرات | `navy-50` | `navy-900` | 13.0:1 |
| أدوات طبية | `teal-50` | `teal-800` | 9.1:1 |
| **أدوات مكتبية** | `amber-50` | `navy-900` | **13.6:1** |

«أدوات مكتبية» تحديدًا كانت معرّفة **ثلاث مرات مختلفة**:
`bg-amber-50 text-amber-700` في موضع، و`from-amber-100 to-amber-200` مع إيموجي
`✏️` في موضع آخر، و`text-amber-600` في ثالث. ثلاثة تعريفات لشيء واحد = ثلاثة
مظاهر لنفس المنتج في ثلاث شاشات.

⚠️ لاحظ أن نص شارة «أدوات مكتبية» **حبر لا ذهبي**: الذهبي `#FFE24B` تعبئة
عالية الإضاءة، وأي نص ذهبي فوقها يسقط تحت العتبة.

### ٦. ممنوع اللون العشوائي

اللون يجب أن يحمل معلومة، أو لا يوجد. خلفية بطاقة المنتج الآن تتبع **تصنيفه**.

### ٧. التدرّج للعمق لا للتلوين

مسموح داخل العائلة الواحدة (`navy→sky` · `sky→sky` · `amber→amber`) أو فوق صورة
لقراءة النص. **ممنوع** التدرّج بين عائلتين مختلفتين لغرض زخرفي.

### ٨. لا إيموجي في الواجهة

الإيموجي يُرسَم بخط النظام: لون خارج اللوحة لا يمكن ضبطه، وشكل مختلف تمامًا بين
أندرويد وiOS وويندوز — أي أنه **غير قابل للاعتماد في تصميم يبني عليه مطوّر**.
البديل أيقونات `lucide-react` تأخذ لونها من `className`.

---

## ما تغيّر فعليًا

| البند | قبل | بعد |
|---|---|---|
| إيموجي في الواجهة | 261 | 0 |
| `violet-*` · `rose-*` | 33 | 0 |
| `emerald-*` (تكرار لـ`teal`) | 91 | 0 |
| `orange` · `yellow` · `slate` · `blue` · `green` · `pink` · `indigo` · `cyan` | 99 | 0 |
| تدرّجات عشوائية حسب `charCode(id)` | 4 مصفوفات | 0 |
| تعريفات لون «أدوات مكتبية» | 3 | 1 |
| خصائص اتجاه فيزيائية (`ml/mr/left/right`) | 272 | 45 (زخرفية أو كسور توسيط) |
| ألوان خارج اللوحة (`audit:ui`) | 0 | 0 |

---

## الحراسة

```bash
npm run audit:ui     # يحرس البند صفر: الخروج عن اللوحة
```

البنود ١–٨ تُحرَس بالمراجعة. إن كسرتَ واحدًا فاذكر السبب في وصف التغيير.

</div>
