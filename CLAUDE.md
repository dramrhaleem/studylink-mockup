# StudyLink Mockup — instructions for Claude

اقرأ هذا الملف بالكامل قبل أي تعديل. كل قاعدة هنا كانت خطأً حقيقيًا في نسخة سابقة،
ومكتوبة هنا تحديدًا لأنها **لا تُكتشف بالنظر إلى الشاشة محليًا**.

---

## ما هذا المشروع

موك اب واجهة تفاعلي لتطبيق **StudyLink** — سوق أكاديمي لطلبة جامعة المنصورة
(مذكرات وملخصات وأدوات، بتوصيل أو استلام من المكتبة). عربي، `RTL`، ٢٧ شاشة داخل
إطار هاتف على صفحة واحدة.

**ليس تطبيقًا عاملًا:** لا باك-إند ولا مدفوعات. البيانات ثابتة في
`src/lib/studylink-data.ts` والحالة في `localStorage`.

**منشور على:** https://dramrhaleem.github.io/studylink-mockup/

**التقنية:** Next.js 16 (App Router) · React 19 · **Tailwind v4** · zustand ·
framer-motion · shadcn/ui · TypeScript strict.

---

## ⚠️ قيود بيئة التشغيل — اقرأها أولًا

**1. `Bash` بتاع كلود وراء بروكسي يمنع GitHub.**
أي `POST` إلى `api.github.com` يرجع 403 برسالة بروكسي Anthropic
(«sessions are bound to their configured repositories») **مهما كان التوكن**.
إنشاء مستودع أو `git push` من هناك **مستحيل** — لا تحاول ولا تطلب توكنًا.

**2. جهاز المستخدم خارج البروكسي.** لو كانت أدوات `mcp__remote-devices__` متاحة،
نفّذ كل عمليات git/GitHub عبر `Desktop_Commander__start_process` — من هناك نجح
إنشاء المستودع والدفع وتفعيل Pages.

**3. النشر لا يحتاج توكنًا أصلًا.** `.github/workflows/deploy.yml` يستخدم OIDC.
أي `git push` إلى `main` ينشر تلقائيًا خلال ~دقيقتين.

**4. القرص خارجي.** لو ظهرت كل الملفات معدّلة في `git status`:
`git config core.fileMode false` (مضبوط بالفعل — لا تلغه).

---

## القواعد الملزمة

كلها تنتج أعطالًا **صامتة**: تعمل محليًا وتنكسر بعد النشر، أو تنكسر بصريًا بلا خطأ
في الكونسول.

### أ. المال

**كل رقم مالي من `src/lib/pricing.ts` فقط.** لا تحسب أي مبلغ داخل شاشة.
كان الرسم مكرّرًا في ثلاثة ملفات وقد اختلفت قيمه فعليًا.

```ts
serviceFee = Σ clamp(round(سلة كل مكتبة × 0.10), 3, 8)   // لكل مكتبة ثم تُجمع
deliveryFee = 25 · pickupFee = 0
```

الأرقام مرتبطة بادعاءات موثّقة في قاعدة معرفة المشروع (`C-001` · `C-002` · `D-032`
· `D-035`). **لا تغيّر رقمًا ماليًا من تلقاء نفسك** — لو التغيير مطلوب، نفّذه واذكر
صراحةً أنه يحتاج تسجيلًا في `core/08-decisions-change-log.md`.

### ب. الاتجاهية (RTL)

- **كل رقم معروض يُلفّ في `.sl-num`:**
  `<span className="sl-num">{price}</span> ج.م`
  بدونها تقلب الخوارزمية ثنائية الاتجاه ترتيب الرقم والعملة.
- **`.sl-num` تحوي رقمًا ووحدة، لا جملة عربية.** خط الأرقام لاتيني أحادي
  المسافة. `--font-mono` يُسقِط العربية على `--font-studylink-arabic` عمدًا
  (راجع التعليق في `@theme`) فلا تنفصل الحروف، لكن الوحدة تظل بلا تشكيل
  جدولي — فاكتب `<span className="sl-num">5</span> إحالة` لا العكس.
- **خصائص منطقية:** `ms-* me-* ps-* pe-* start-* end-*` — لا `ml-* mr-* left-* right-*`.
- **`translateX` فيزيائية لا منطقية.** في RTL الحركة للأمام **سالبة**:
  `animate={{ x: on ? -20 : 0 }}`. (كان مفتاح التبديل يختفي بسبب هذا في ٣ مواضع.)
- **ممنوع `tracking-*` على العربية** — أي `letter-spacing` موجب يكسر الوصل.
- **أرقام غربية `0-9` دائمًا.** ممنوع `toLocaleString('ar-EG')`.

### ج. الأصول

**أي مسار في `public/` يمرّ عبر `asset()`** من `src/lib/asset.ts`:

```tsx
<Image src={asset('/products/anatomy-notes.png')} … />
```

`basePath` لا يُطبَّق تلقائيًا على `next/image` ولا على أيقونات `metadata`. المسار
المطلق يعمل محليًا و**يصير 404 على الموقع المنشور**. كانت ١٧٧ إشارة تسقط بهذه الطريقة.

**الخطوط عبر `next/font/local`** (`src/app/fonts.ts`) — لا `@font-face` بمسار مطلق،
لنفس السبب. سقوط الخط لا ينتج أي خطأ، فقط عربية بخط النظام.

### د. الألوان

**كل التوكنز في `src/app/globals.css` داخل `@theme`.** لا تكتب لونًا في ملف شاشة،
ولا قيمة hex في أي مكان.

اللوحة **مغلقة**: نطاقات Tailwind القياسية (`emerald` `violet` `rose` `slate` …)
أُعيد تعريفها لتشير إلى ألوان البراند. لو احتجت لونًا جديدًا، أضفه في `@theme` أولًا.

`npm run audit:ui` يفشل الرقم `off-brand colours` لو خرج شيء عن اللوحة.

**لكن الإغلاق التقني لا يكفي.** كانت الواجهة تستعمل ست عائلات لونية في شاشة
واحدة من داخل اللوحة، فبدت «كمدينة ملاهٍ». القانون الدلالي ملزم بنفس القدر —
تفصيله في **`DESIGN-SYSTEM-COLOR.md`**، وخلاصته:

1. `navy-*` و`brand-grey-*` يحملان الواجهة. لو أزلت باقي الألوان يجب أن تظل مفهومة.
2. **`sky-*` هو اللهجة التفاعلية الوحيدة.** لا لون آخر يعني «اضغط».
3. `success` · `warning` · `error` **للحالة فقط**. «جديد» معلومة لا نجاح → سماوي.
4. `amber-*` لشيئين: مكافأة السفير · شارة «أدوات مكتبية». لا ثالث.
5. **لون التصنيف من `src/lib/category.ts` وحده** — ثلاثة أزواج AAA ولا رابع.
6. **ممنوع اللون العشوائي.** كان لون البطاقة `gradients[charCode(id) % 5]`.
7. التدرّج داخل العائلة الواحدة فقط (`navy→sky`)، أو فوق صورة لقراءة النص.
8. **لا إيموجي في الواجهة** — لون خارج اللوحة يختلف بين أندرويد وiOS.
   استخدم `lucide-react`. (أُزيل 261 إيموجي.)

### هـ. التخطيط

- **الحد الأدنى للخط:** 12px لعناصر الواجهة، 13px للمتن.
- **الأزرار الصغيرة تأخذ `data-tap="44"`** — يوسّع منطقة اللمس بلا تغيير الشكل.
- **الشرائط الأفقية تأخذ `rail-gutter`** — حشو نهاية حاوية التمرير تُسقطه محرّكات
  التخطيط، فآخر بطاقة تُقص على حافة الشاشة.
- **ممنوع `scrollIntoView` داخل كاروسيل أو قائمة رسائل.** الدالة تمرّر **كل**
  الحاويات الأب حتى يظهر العنصر، لا الحاوية المقصودة وحدها — و`block: 'nearest'`
  لا يعصمك. كان مؤقّت البانرات في الرئيسية ينادي `scrollIntoView` كل ٤ ثوانٍ،
  فيسحب المستخدم لأعلى الصفحة كلما نزل. البديل: مرّر الحاوية نفسها بـ`scrollBy`
  بفارق مركزَي العنصر والحاوية المحسوب من `getBoundingClientRect` — لا يعتمد على
  إشارة `scrollLeft` (تختلف بين المتصفحات في RTL) ولا يلمس أي حاوية أخرى.
  `npm run audit:scroll` يحرس هذا.

### و. المحتوى — بوابة الادعاءات

المشروع محكوم بقواعد رسائل في `core/07-brand-product-experience.md §4`. **ممنوع**:

- زمن توصيل أو رد ثابت بلا سجل `P50/P90`
- «مضمون» · «الأسرع» · «أصلي 100%» · «مجاني» بلا تحديد البند
- أي عدد مستخدمين أو طلبات أو تقييم **غير مولّد من النظام**
- سياسة إرجاع أو شراكة أو ضمان قبل مستند نافذ
- سعر مرجعي مشطوب لم تتقاضَه المنصة فعلًا

كانت شاشة «عن التطبيق» تعرض `2,000+ طالب` و`8,500+ مذكرة بِيعت` — أرقام مُختلقة
بالكامل والواقع صفر طلبات من خارج المعارف. **لا تُنشئ أرقامًا تسويقية.**

### ز. الاسم

**`StudyLink` باللاتينية في كل السياقات، عربية كانت أم إنجليزية.**
«ستادي لينك» **مرفوضة** («ستاد» = الملعب، و«ستادي» تُقرأ «ملعبي»).
النقحرة المعتمدة «ستدي لينك» لكلمات البحث المخفية فقط، لا كاسم معروض.

---

## خريطة الملفات

| تريد تغيير | اذهب إلى |
|---|---|
| لون · خط · زاوية · ظل · وضع داكن | `src/app/globals.css` (`@theme`) |
| **لون/أيقونة تصنيف منتج** | `src/lib/category.ts` |
| **محتوى بطاقة «العملي في الجيب»** | `src/lib/season.ts` (`ACTIVE_SEASON`) |
| **مراحل الطلب وشريط الطلب الجاري** | `src/lib/order-status.ts` · `ActiveOrderBar.tsx` |
| **مواصفة شاشة للمطوّر** | `src/lib/spec.ts` |
| سعر · رسم · عرض تشغيلي | `src/lib/pricing.ts` |
| منتج · مكتبة · بيانات | `src/lib/studylink-data.ts` |
| شاشة | `src/components/studylink/screens/<Name>Screen.tsx` |
| تسجيل شاشة جديدة | `src/app/page.tsx` → `renderSecondaryScreen` |
| شريط التنقّل | `src/components/studylink/BottomNavBar.tsx` |
| بطاقة الحساب | `src/components/studylink/screens/PricingBreakdown.tsx` |
| حالة (سلة · مستخدم · إشعارات) | `src/lib/use-studylink-store.ts` |
| صور المنتجات والبانرات | `scripts/gen-*-art.mjs` (متولّدة من كود، لا مرفوعة) |

**لا يوجد `tailwind.config.ts`** — Tailwind v4 يقرأ الإعداد من CSS. راجع `TAILWIND-NOTE.md`.

---

## طبقة المطوّر داخل الموك اب

الموك اب يُسلَّم لمطوّر لا يعرف قرارات المنتج، فكان يعود بأسئلة إلى المؤسس عن
كل تفصيلة. الإجابة صارت **داخل الموك اب نفسه**:

زر **«مواصفات للمطوّر»** أسفل إطار الهاتف يفتح لوحة تعرض، للشاشة المعروضة:

| القسم | ماذا يجيب |
|---|---|
| الغرض | لماذا توجد هذه الشاشة |
| مصدر البيانات | من أي ملف تأتي الآن، وما يقابله في المنتج |
| قواعد ملزمة | ما لا يجوز كسره عند البناء |
| حالات يجب بناؤها | فارغ · تحميل · خطأ · صلاحيات |
| **مؤجَّل — لا يُبنى الآن** | ما هو معروض لكنه خارج هذه الجولة |
| معايير القبول | متى تُعتبر الشاشة «تمّت» |

المحتوى كله نص في **`src/lib/spec.ts`** ولا يؤثر على سلوك التطبيق، وهو خارج
إطار الهاتف فلا يراه المستخدم النهائي.

**⚠️ حين تضيف شاشة، أضف مواصفتها في `spec.ts`.** شاشة بلا مواصفة = سؤال جديد
للمؤسس، وهو بالضبط ما بُنيت هذه الطبقة لمنعه.

---

## دورة العمل

```bash
npm run setup      # مرة واحدة — install --include=dev + كروميوم للأوديت
npm run dev        # http://localhost:3000
```

**⚠️ `NODE_ENV=production` مضبوط عالميًا على جهاز المستخدم.** `npm install` العادي
يتخطّى `devDependencies` بصمت فيفشل البناء برسالة مضلِّلة
(`Cannot find module '@tailwindcss/postcss'`). استخدم `npm run setup` دائمًا.
ولا تُصدّر `NODE_ENV=development` قبل البناء — Next يضبطها بنفسه، وإجبارها يكسر
prerender الصفحة `/_global-error`.

شاشة بعينها: `http://localhost:3000/?screen=checkout`

### التحقق — إلزامي قبل أي تسليم

```bash
npm run typecheck                  # لا يُقبل أي خطأ (لا تستخدم npx tsc — يجلب حزمة خاطئة)
npm run lint                       # صفر أخطاء — أي تعطيل موضعي مكتوب بسببه
npm run test:pricing               # قواعد المال مقابل الإفادة المسجّلة (D-032 · D-035)
npm run audit:ui                   # يحتاج dev شغّالًا
npm run audit:overlap              # نص يمرّ تحت زر مطلق — لا ينتج خطأً ولا يظهر في أي فحص آخر
npm run audit:scroll               # سرقة التمرير: شاشة تعيد المستخدم لأعلى وحدها
STATIC_EXPORT=1 npm run build      # نفس بناء GitHub بالضبط
```

القيم المرجعية لـ`audit:ui` — **أي ارتفاع = انحدار في تعديلك**:

```
contrast failures : 3     ← إيجابيات كاذبة: نص أبيض فوق تدرّج، وزر معطّل
touch < 44px      : 5     ← 3 نقاط كاروسيل لها توسيع خاص + حقلا إدخال داخل حاوية 44px
unnamed controls  : 0     ← يجب أن يبقى صفرًا
horizontal overflow: 0    ← يجب أن يبقى صفرًا
off-brand colours : 0     ← يجب أن يبقى صفرًا
```

لقطات للمقارنة البصرية:

```bash
npm run shots        # كل الشاشات
npm run shots:dark   # الوضع الداكن
npm run shots:cart   # شاشات المال بسلة مزروعة (السلة فاضية افتراضيًا)
```

### النشر

```bash
git add -A && git commit -m "…" && git push
```

عبر `Desktop_Commander` لا عبر `Bash` (راجع قيود البيئة أعلاه).

---

## أعراض ← أسباب

| العرض | السبب |
|---|---|
| صور تعمل محليًا وتسقط بعد النشر | مسار بلا `asset()` |
| عربية بحروف منفصلة أو متباعدة | `.sl-num` على نص عربي، أو `tracking-*` |
| الرقم والعملة مقلوبان | `.sl-num` ناقصة |
| عنصر تحرّك للجهة المعاكسة | `translateX` موجب في RTL |
| الخط يبدو مختلفًا بعد النشر بلا خطأ | `@font-face` بمسار مطلق بدل `next/font` |
| البناء يفشل على GitHub ويعمل محليًا | جرّب `STATIC_EXPORT=1` — التصدير الثابت أصرم |
| الصفحة ترجع لأعلى وحدها أثناء التمرير | `scrollIntoView` داخل مؤقّت — استبدله بـ`scrollBy` على الحاوية |
| شريط أو بطاقة تظهر بعد التركيب فيقفز ما تحتها | حالة تعتمد على `Date.now()` تُقرأ في `useEffect` — ارسم بقيمة أولية آمنة ثم حدّثها |
| كل الملفات تبدو معدّلة | `git config core.fileMode false` |

---

## ملفات مرجعية

| الملف | المحتوى |
|---|---|
| `UPGRADE-REPORT.md` | **اقرأه قبل أي تعديل أساسي** — كل باج أُصلح ولماذا |
| `DESIGN-SYSTEM-COLOR.md` | **قانون اللون** — متى يُستخدم اللون، لا أيّه موجود |
| `DEV.md` | دليل التعديل للبشر |
| `DESIGN-SYSTEM.md` | نظام التصميم بالتفصيل |
| `README.md` | نظرة عامة |

مصدر الهوية: `deliverables/brand/studylink-identity-v1` في قاعدة معرفة المشروع
(خارج هذا المستودع). القرارات المعتمدة: `D-032` رسوم الخدمة · `D-033` الهوية ·
`D-035` نطاق الاحتساب.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
