<div dir="rtl">

# تسليم StudyLink — ابدأ من هنا

هذه الحزمة هي كل ما يحتاجه المطوّر لبناء تطبيق **StudyLink** الحقيقي: الموك اب
التفاعلي بكامل كوده، وهوية بصرية معتمدة، ومواصفة مكتوبة لكل شاشة.

**الموك اب منشور:** <https://dramrhaleem.github.io/studylink-mockup/>
**المستودع:** <https://github.com/dramrhaleem/studylink-mockup>

---

## ١. أول ١٠ دقائق

```bash
npm run setup      # مرة واحدة — install --include=dev + كروميوم لأدوات الفحص
npm run dev        # http://localhost:3000
```

ثم افتح الصفحة واضغط **«مواصفات للمطوّر»** تحت إطار الهاتف.

هذه اللوحة هي أهم شيء في التسليم. تعرض لكل شاشة:

| القسم | ماذا يجيب |
|---|---|
| **الغرض** | لماذا توجد هذه الشاشة أصلًا |
| **مصدر البيانات** | من أي ملف تأتي الآن، وما يقابله في المنتج الحقيقي |
| **قواعد ملزمة** | ما لا يجوز كسره عند البناء |
| **حالات يجب بناؤها** | فارغ · تحميل · خطأ · صلاحيات |
| **مؤجَّل — لا يُبنى الآن** | ما هو معروض لكنه خارج هذه الجولة |
| **معايير القبول** | متى تُعتبر الشاشة «تمّت» |

المحتوى نصّ في `src/lib/spec.ts`. **شاشة جديدة بلا مواصفة = سؤال جديد للمؤسس.**

---

## ٢. ما هذا وما ليس هو

**هو:** موك اب واجهة تفاعلي كامل — ٢٧ شاشة، عربي `RTL`، مبني على الهوية الرسمية،
يعمل فعليًا في المتصفح، وكل تدفّقاته قابلة للنقر.

**ليس:** تطبيقًا عاملًا. لا باك-إند ولا مدفوعات ولا حساب حقيقي. البيانات ثابتة في
`src/lib/studylink-data.ts` والحالة في `localStorage`.

**استخدمه كمواصفة تنفيذية**، لا كقاعدة كود تُبنى فوقها مباشرة: التقنية هنا
(Next.js للويب) اختيار للمعاينة، والمنتج الحقيقي قد يكون تطبيقًا أصليًا.
**الملزِم هو السلوك والقواعد والهوية**، لا الإطار.

---

## ٣. مصادر الحقيقة — كل شيء له مكان واحد

| الموضوع | الملف | لماذا واحد |
|---|---|---|
| **المال** | `src/lib/pricing.ts` | كان الرسم مكرّرًا في ٣ ملفات وقد **اختلفت قيمه فعليًا** |
| **الألوان والخطوط والزوايا** | `src/app/globals.css` (`@theme`) | لوحة مغلقة — حتى نطاقات Tailwind القياسية أُعيد تعريفها على البراند |
| **ألوان التصنيفات** | `src/lib/category.ts` | «أدوات مكتبية» كانت معرّفة **٣ مرات مختلفة** في ٨ ملفات |
| **مراحل الطلب** | `src/lib/order-status.ts` | كانت مكتوبة في ٣ مواضع بصياغات مختلفة |
| **المدخل الموسمي** | `src/lib/season.ts` | سطر `ACTIVE_SEASON` واحد يغيّر البطاقة كلها |
| **البيانات** | `src/lib/studylink-data.ts` | منتجات · مكتبات · دكاترة · مواد |
| **الحالة** | `src/lib/use-studylink-store.ts` | zustand + `persist` |
| **مواصفات الشاشات** | `src/lib/spec.ts` | ما يقرأه المطوّر بدل سؤال المؤسس |

---

## ٤. قواعد المال — لا تُشتق في شاشة

```ts
serviceFee  = Σ clamp(round(سلة كل مكتبة × 0.10), 3, 8)   // لكل مكتبة ثم تُجمع
deliveryFee = 25            pickupFee = 0
```

**رسم الخدمة يُحتسب لكل مكتبة على حدة ثم يُجمع** (`D-035`). أثر ذلك:

| السلة | الرسم |
|---|---|
| 88 من مكتبة واحدة | 8 |
| 50 هارفرد + 38 برلين (نفس الـ88) | 5 + 4 = **9** |
| 135 + 76 | 8 + 8 = **16** (السقف الفعلي لطلب مكتبتين) |

الاستلام من المكتبة رسم تنفيذه صفر، **لكن رسم الخدمة يظل مستحقًا**.

```bash
npm run test:pricing    # يتحقق من كل هذه الحالات آليًا
```

⚠️ الأرقام مرتبطة بقرارات موثّقة (`C-001` · `C-002` · `D-032` · `D-035`).
**لا تغيّر رقمًا ماليًا من تلقاء نفسك.**

---

## ٥. القواعد التي تنتج أعطالًا صامتة

كلها كانت أخطاءً حقيقية. تعمل محليًا وتنكسر بعد النشر، أو تنكسر بصريًا بلا خطأ
في الكونسول.

1. **كل رقم معروض يُلفّ في `.sl-num`** — بدونها تقلب الخوارزمية ثنائية الاتجاه
   ترتيب الرقم والعملة. ولا تضعها على جملة عربية كاملة.
2. **كل أصل من `public/` يمرّ عبر `asset()`** — `basePath` لا يُطبَّق تلقائيًا،
   فتسقط الصور بصمت بعد النشر. كانت ١٧٧ إشارة تسقط هكذا.
3. **الخطوط عبر `next/font/local`** لا `@font-face` بمسار مطلق — لنفس السبب.
4. **RTL بخصائص منطقية:** `ms/me/ps/pe/start/end`. و`translateX` **فيزيائية** —
   في RTL الحركة للأمام سالبة.
5. **ممنوع `tracking-*` على العربية** — أي `letter-spacing` موجب يكسر الوصل.
6. **أرقام غربية `0-9` دائمًا.** ممنوع `toLocaleString('ar-EG')`.
7. **ممنوع `scrollIntoView` داخل كاروسيل أو قائمة رسائل** — يمرّر كل الحاويات
   الأب فتقفز الصفحة لأعلى. استخدم `scrollBy` على الحاوية نفسها.
8. **الحد الأدنى للخط 12px** لعناصر الواجهة و13px للمتن.
9. **الاسم المعروض `StudyLink` باللاتينية دائمًا** — «ستادي لينك» مرفوضة.
10. **بوابة الادعاءات:** ممنوع زمن توصيل ثابت، أو «مضمون»، أو أي عدد
    مستخدمين/طلبات غير مولّد من النظام.

---

## ٦. اللون — قانون من ٨ بنود

اللوحة مغلقة تقنيًا، **لكن ذلك لا يكفي**. كانت الواجهة تستعمل ست عائلات لونية في
الشاشة الواحدة من داخل اللوحة، فبدت «كمدينة ملاهٍ». التفصيل الكامل في
[`DESIGN-SYSTEM-COLOR.md`](DESIGN-SYSTEM-COLOR.md)، وخلاصته:

1. الحبر والورق يحملان الواجهة — أزل باقي الألوان ذهنيًا، يجب أن تظل مفهومة.
2. **`sky` هو اللون التفاعلي الوحيد.** لا لون آخر يعني «اضغط».
3. `success`/`warning`/`error` **للحالة فقط**. «جديد» معلومة لا نجاح → سماوي.
4. `amber` لشيئين: مكافأة السفير · شارة «أدوات مكتبية». لا ثالث.
5. لون التصنيف من `category.ts` وحده — ثلاثة أزواج AAA ولا رابع.
6. **ممنوع اللون العشوائي.** كان لون البطاقة `gradients[charCode(id) % 5]`.
7. التدرّج داخل العائلة الواحدة فقط، أو فوق صورة لقراءة النص.
8. **لا إيموجي في الواجهة** — لون خارج اللوحة يختلف بين أندرويد وiOS.

---

## ٧. الهوية البصرية

| ما تريد | أين |
|---|---|
| **البراند بوك الكامل** (٣١ قسمًا) | `public/brand/studylink-brand-book.html` |
| **أيقونات النظام** (٣٥ SVG) | `public/brand/icons/` — `currentColor` وسمك 2 |
| **حزم التنزيل** (شعار · خطوط · توكنز · قوالب) | `public/brand/downloads/*.zip` |
| **نسخة سحابية** | [مجلد Google Drive](https://drive.google.com/drive/folders/1BX9YVdAvYwxu-eWqCweIvsKJKMwOcDYZ) |

⚠️ `public/brand/` **نسخة** من `deliverables/brand/studylink-identity-v1`.
لا تُعدَّل يدويًا — تُعاد نسختها عند تحديث المصدر.

---

## ٨. التحقق — إلزامي قبل أي تسليم

```bash
npm run typecheck                  # لا يُقبل أي خطأ
npm run lint                       # صفر أخطاء
npm run test:pricing               # قواعد المال
npm run audit:ui                   # تباين · لمس · أسماء وصول · تجاوز · ألوان
npm run audit:ux                   # نص مقصوص · تحت 12px · شاشة بلا مخرج
npm run audit:overlap              # نص يمرّ تحت زر مطلق
npm run audit:scroll               # سرقة التمرير
STATIC_EXPORT=1 npm run build      # نفس بناء GitHub بالضبط
```

**القيم المرجعية — أي ارتفاع = انحدار في تعديلك:**

```
contrast failures : 3     ← إيجابيات كاذبة (نص أبيض فوق تدرّج) وزر معطّل
touch < 44px      : 5     ← 3 نقاط كاروسيل + حقلا إدخال داخل حاوية 44px
unnamed controls  : 0     ← يجب أن يبقى صفرًا
horizontal overflow: 0    ← يجب أن يبقى صفرًا
off-brand colours : 0     ← يجب أن يبقى صفرًا
```

الفحوص الثلاثة الأخيرة (`ux` · `overlap` · `scroll`) بُنيت خصيصًا لأعطال
**لا تنتج خطأً في الكونسول ولا تظهر في أي فحص آخر**.

---

## ٩. ما هو مؤجَّل — لا يُبنى في هذه الجولة

| البند | الحالة |
|---|---|
| **تقييم المنتجات** | **أُزيل بالكامل** بقرار المؤسس — لا يُعاد |
| **إنجازاتي** | معروضة موسومة «ميزة مؤجلة». أرقامها ولوحة متصدّريها **تجريبية مُختلقة** — لا تُنقل للإنتاج |
| **التتبع على الخريطة** | مؤجَّل. لا GPS ولا مصدر موقع، فأي خريطة حيّة ادعاء بلا سند |
| **الاسترجاع** | لا مستند نافذ — النص يقول إنه تحت التفعيل |
| **بوابة الدفع** | الشاشة تحاكي النتيجة فقط |

---

## ١٠. قرارات تحتاج المؤسس قبل البناء

| السؤال | لماذا يهم |
|---|---|
| **مصدر مرحلة الطلب** — webhook من المكتبة؟ لوحة تحكم يدوية؟ | يحدّد شكل الـAPI بالكامل. حاليًا مشتقّة من الزمن **للمعاينة فقط** (`demoStageIndex`) — احذفها عند الوصل |
| **مصدر «الموسم»** — إعداد خادم أم تقويم أكاديمي؟ | يحدّد من يبدّل بطاقة «العملي في الجيب» ومتى |
| **تاريخ مراجعة تأجيل «إنجازاتي»** | غير محدّد حاليًا |

---

## ١١. خريطة الملفات الكاملة

| تريد تغيير | اذهب إلى |
|---|---|
| لون · خط · زاوية · ظل · وضع داكن | `src/app/globals.css` (`@theme`) |
| سعر · رسم · عرض تشغيلي | `src/lib/pricing.ts` |
| لون/أيقونة تصنيف | `src/lib/category.ts` |
| مراحل الطلب وشريط الطلب الجاري | `src/lib/order-status.ts` · `ActiveOrderBar.tsx` |
| محتوى «العملي في الجيب» | `src/lib/season.ts` (`ACTIVE_SEASON`) |
| مواصفة شاشة | `src/lib/spec.ts` |
| منتج · مكتبة · بيانات | `src/lib/studylink-data.ts` |
| شاشة | `src/components/studylink/screens/<Name>Screen.tsx` |
| تسجيل شاشة جديدة | `src/app/page.tsx` → `renderSecondaryScreen` |
| شريط التنقّل | `src/components/studylink/BottomNavBar.tsx` |
| بطاقة الحساب | `src/components/studylink/screens/PricingBreakdown.tsx` |
| الحالة | `src/lib/use-studylink-store.ts` |
| صور المنتجات والبانرات | `scripts/gen-*-art.mjs` (متولّدة من كود) |

**لا يوجد `tailwind.config.ts`** — Tailwind v4 يقرأ الإعداد من CSS.

---

## ١٢. المستندات

| الملف | المحتوى |
|---|---|
| **`CLAUDE.md`** | تعليمات وكيل الذكاء الاصطناعي — أشمل مرجع تقني، اقرأه لو ستسلّم المشروع لكلود |
| `DEV.md` | دليل التعديل للبشر — «عايز أغيّر كذا ← روح فين» |
| `DESIGN-SYSTEM-COLOR.md` | قانون اللون الثمانيّ بالتفصيل والقياسات |
| `UPGRADE-REPORT.md` | الجولة ١ — البنية والهوية والنشر، وكل باج أُصلح ولماذا |
| `ROUND-2-REPORT.md` | الجولة ٢ — السلوك واللون وطبقة المطوّر |
| `TAILWIND-NOTE.md` | لماذا لا يوجد ملف إعداد جافاسكريبتي |
| `README.md` | نظرة عامة والتشغيل |

---

## ١٣. النشر

```bash
git add -A && git commit -m "…" && git push
```

`.github/workflows/deploy.yml` يبني وينشر تلقائيًا على كل دفعة إلى `main`،
عبر OIDC **بلا أي توكن**. الموقع يتحدّث خلال ~دقيقتين.

`basePath` يُحسب تلقائيًا من اسم المستودع، فالمشروع يعمل سواء نُشر في مجلد
مستودع أو على النطاق الجذر.

</div>
